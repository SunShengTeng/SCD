#端口号
server:
  port: 8080
spring:
  application:
    name: item-service

eureka:
  instance:
    hostname: item
  #客户端
  client:
    #注册中心地址
    service-url:
      defaultZone: http://localhost:8888/eureka/,http://localhost:8889/eureka/